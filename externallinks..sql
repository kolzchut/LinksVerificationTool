SELECT DISTINCT `el_to` FROM `externallinks` 
;and then export everything, check using Xenu
;maybe!: select el_from for every broken link later.
;alternatively: make volunteer use special:חיפוש קישורים יוצאים
